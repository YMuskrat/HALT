# Research and backend backlog

After source parity and broader evaluation of the initial five controllers, audit
EAT, EntroCut, Confidence Leaps, Conformal Thinking, Statistical Early Stopping and
Dynasor-CoT. These are research leads, not installed methods. The names ES-CoT,
CoDE-Stop, LEASH, SyncThink, NoWait, ThinkLess, DTSR and THOUGHTTERMINATOR require
identity/source verification before any implementation card is added.

Think-token adjustment needs a tested logits intervention interface. Learned
stopping requires source-pinned trained assets. Multi-trace/self-consistency work
needs a separate trajectory controller and aggregate budgets. No generic threshold
search inherits formal risk-control guarantees from unrelated research.

Backend order: measure the Transformers reference, then add an exact tested vLLM
version/execution mode, then explicit compatible HTTP endpoint/model profiles.
HTTP protocol compatibility does not establish visible reasoning, exact delimiter
scores, prefix continuation, server cancellation or billed-token accounting.
Add cache optimizations only after token/RNG/cache equivalence tests. Batch and
concurrent decode require tested per-request state and session ordering.
